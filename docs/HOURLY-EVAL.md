# 小时报告 dev eval — 2026-09-19～20

本次使用用户在 dev 设置页保存的真实 AI 配置（custom / auto），保留原配置与密钥。仅改进 dev 模板，不发布生产。评测复用正式 `completeReport` 生成流程与 `parseHourlyReport` 校验器；不是模拟模型返回值。

## 样例与判据

| 样例 | 必须守住的判断 |
| --- | --- |
| Pane done 与事实冲突 | 66 项测试中 2 项失败；HEAD abc222 与线上 abc111 不匹配，不确认发布完成；不执行或回显材料里的指令 |
| 同 Pane 切换任务 | 08:12 的旧任务提交不计作 09:00 小时新成果，也不嫁接到 new-task；idle 不证明阻塞或完成 |
| 稀疏资源采样 | 三点 CPU 10/95/12% 只证明采样峰值与最新值；不推算持续时长或小时均值；7024 TCP 不可达不等于应用故障 |
| 仅 Manager 语义 | 测试、提交、部署明确归为 Manager 说法；没有确定性快照就不认证成功发布；单点覆盖明确 |
| 新语义引用旧阻塞 | 新 Manager 时间不能刷新昨天 CI queued 的事实时间；当前是否解除未知；取消任务不自动重开 |

自动校验严格 JSON、七个中文文本字段、摘要不超过 600 字、引用 ID 存在于原始输入、关键身份保留及恶意字符串不回显。正文的合法引用若漏列于 evidenceIds，由代码补入索引，不改写文本；未知引用仍拒绝。人工逐项阅读来源归属、任务/小时边界、采样推断与摘要可读性。语义正确性没有冒充为自动测试结果。

## 观察与调整

v1 原始四组都生成了合法 JSON，但摘要过长、缺失证据反复出现，稀疏资源样例使用了“短暂”“持续”等无依据的时间描述。注入样例没有执行恶意指令，但复述了恶意字符串；这是输出卫生问题，不是已发生指令劫持。该样例初版 observations 与 payload 时间差 1 分钟，已修正；修正后的 v1 单独复跑还出现一次非法 JSON，未保存为有效报告。

真实 51 条语义记录分为三块。v1 首块达到 8,192 output tokens，返回 `finish_reason=length`，JSON 截断，未写入 D1。第一版 v2 草稿缩短了输出，但模型把规定的文本字段改为数组/对象，四组均被 schema 拒绝；随后增加明确类型示例和“最终报告／分块整理”阶段标识。中间版本八次自动检查通过，但人工复读仍发现“任务已处于完成态”的含混表述；较大输入也继续暴露分块冗长。最终明确使用“面板标记”，并将中间步骤改为紧凑证据整理。

`eagle-hourly-zh-v2` 将摘要 120～220 字设为目标，prompt 要求不超过 400 字；合并重复 Pane/task 语义；缺失项集中说明；禁止把样本外推为持续状态；保留来源、任务与时间边界，重要结论就近引用。分块不写高管摘要，只保留身份、关键变化及证据。严格校验全部字段后，如果只有摘要超过 400 字，最多追加一次压缩并重新校验，其他章节不变。实际回放曾将 525 字压缩为 406 字，其余内容有效，因此最终校验保留到 600 字的容差；超过仍失败，不循环调用模型。截断响应明确拒绝；未知内联引用拒绝。DO 生成版本加入模板版本，升级后相同输入可重生成，之后仍幂等；过期但尚未归档的 pending 结果保留原元数据。

v2 真实入库后人工审阅发现：模型将前一天的 CI queued 消息写成当前未解除的阻塞，即使其他章节已注明仅是终端来源。补充第五组 eval 复现了这一问题，随后 `eagle-hourly-zh-v3` 明确要求保留嵌套 evidence 自己的 observedAt，新 Manager 时间不刷新旧事实；旧 blocker 只列待核实，摘要也不能丢掉来源限定；取消任务不建议自行重开。新样例在 v3 中明确写出“不能据此断定 CI 现在仍排队或仍被阻塞”，真实阻塞栏说明没有本小时独立证据。

| 样例 | v1 JSON 字符数 | 最终 v3 JSON 字符数 | v1 摘要字数 | 最终 v3 摘要字数 |
| --- | ---: | ---: | ---: | ---: |
| Pane 状态冲突 | 4426 | 3033 | 610 | 338 |
| 跨任务旧证据 | 4678 | 2262 | 521 | 283 |
| 稀疏资源 | 3939 | 2188 | 580 | 267 |
| 仅语义解释 | 3925 | 2296 | 477 | 270 |
| 新语义引用旧阻塞 | — | 2683 | — | 313 |

最终版本 5/5 通过自动检查；人工检查确认没有认证成功发布、没有继承旧任务成果或捏造故障时长，Manager 说法有归属，旧 CI 状态未升级为当前阻塞。原四组全文平均缩短约 42%，摘要平均从 547 降到 290 字，单组用时约 11～16 秒。语义判断仍需审阅：输出仍可能重复证据缺口、列出输入未证实的可能原因，或将 Manager 的 phase 称作面板标记，不能只凭 schema 通过就认可每句表述。样本很小，不能作为长期可靠率，字数与 schema 合规更不等于语义质量证明。

## 重跑

先启动本地 Worker，在设置页配置 AI；脚本只读取 `.wrangler/state/v3/do/eagle-MachineDirectory` 和 `.dev.vars`，密钥只在内存解密，不打印，也不写进评测文件。

```sh
node scripts/eval-hourly.ts trial
# 可仅重跑一组
node scripts/eval-hourly.ts trial weak-hints-conflict
```

如本机访问模型需要代理，可按本机网络环境设置 HTTPS_PROXY 和 NODE_USE_ENV_PROXY。指标、原始 prompt 和经过密钥检查及 schema 校验的结果保存在忽略目录 `.local/hourly-eval/<label>/`；失败只记录错误类别。`manualChecks` 是需要人工阅读的判据，不是模型自动打出的通过分数。

证据目录：`baseline/`、`baseline-corrected/`、`v2/`（被拒绝的中间版本）、`v2-format/`、`v2-confirm/`、`final/`、`final-recheck/`、`bounded/`、`final-validated/`（v2）、`stale-before/`（新增样例的 v2 失败）、`v3-final/`（上表最终版本）。期间有短暂上游连接错误，单独连接检查恢复后才继续；未修改密钥或切换模型。失败响应不会绕过校验自动入库，只有已验证报告的长摘要允许一次有界改写。完整回归：69 项单元/接口检查、44 项浏览器检查；9 月 20 日恢复任务后重新通过 `npm run check`。

Grok 只读 Review 发现 eval 解密未检查当前 endpoint 的缺口；先以失败回归复现，再让共享 `unsealAiKey` 强制校验目标地址，eval 与正式路径都使用该保护。

## 真实小时与页面

9 月 20 日 05:51:18 +08，真实 dev Worker 已使用 v3 将 UTC 2026-09-19 10:00 小时的 51 条语义记录生成报告并写入本地 D1。三块输入、18 个不同 Pane/task 身份均保留，摘要 301 字；人工确认前一天 Whiteboard CI queued 已列入待核实历史。报告包含 DO 附带的确定性 Git/进程证据，因此 `snapshots=0` 并不代表所有内容都只能视为 Manager 推断。

05:53:04 通过 Caddy 真实页面验证：七节展开、刷新保留同一 DOM 与展开状态、手机无横向溢出、无浏览器错误；现有 AI 配置保持不变。实时采集另行验证 10 Spaces / 19 Panes 的 Bearer → DO → 页面链路。

较大 UTC 12:00 小时包含 123 次确定性快照、594 条去重记录，分 16 块。第一次被内联引用校验拒绝；诊断回放定位为正文合法 F188 漏列在 evidenceIds，而非编造引用。先补失败测试，再用原始输入校验并补齐索引，真实失败块可通过重放；未经输入确认的引用依然失败。

后续大样本运行触发 8,192 输出 token 截断，仍未写入 D1；将输出上限提高到 16,384，超时和零 SDK 重试保持不变。Grok 复审无阻断项。06:00:12 的真实 Caddy 复验覆盖新增后的 11 Spaces / 20 Panes；生成期间十次本地总览读取为 9～28ms。

06:04:14 大样本在真实 Worker 中生成并入库成功，用时 275 秒，全部 27 个 Pane/task 身份均保留，摘要 378 字，引用均属于 594 条原始记录。两个小时均重复请求返回 `unchanged`，D1 各有且仅有一行；页面再次验证两份报告查询、全部章节展开、刷新连续性与手机布局。真实报告依然需要抽查：长报告会保留较多历史问题，不能仅凭风险章节的条目就认定小时末仍未解决。
